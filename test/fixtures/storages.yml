source_bucket:
  provider: cloudflare
  bucket_name: "my-source-bucket"

destination_bucket:
  provider: backblaze
  bucket_name: "my-backup-bucket"

shared_bucket:
  provider: amazon
  bucket_name: "shared-bucket"
  display_name: "Daily Backups"

source_only_storage:
  provider: cloudflare
  bucket_name: "production-bucket"
  usage_type: source_only
  display_name: "Production Data"

destination_only_storage:
  provider: backblaze
  bucket_name: "archive-bucket"
  usage_type: destination_only
  display_name: "Archive Storage"
