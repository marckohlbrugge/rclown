<nav class="bg-white border-b border-gray-200">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 justify-between">
      <div class="flex">
        <div class="flex shrink-0 items-center">
          <%= link_to root_path, class: "text-xl font-bold text-gray-900" do %>
            Rclown
          <% end %>
        </div>
        <div class="hidden sm:ml-8 sm:flex sm:space-x-8">
          <%= link_to "Backups", backups_path, class: nav_link_classes(controller_name == "backups" && action_name != "index" ? false : request.path.start_with?("/backups")) %>
          <%= link_to "Providers", providers_path, class: nav_link_classes(request.path.start_with?("/providers")) %>
          <%= link_to "Storages", storages_path, class: nav_link_classes(request.path.start_with?("/storages")) %>
          <%= link_to "Health", health_path, class: nav_link_classes(request.path.start_with?("/health")) %>
        </div>
      </div>
    </div>
  </div>

  <%# Mobile menu %>
  <div class="sm:hidden border-t border-gray-200">
    <div class="space-y-1 px-2 pb-3 pt-2">
      <%= link_to "Backups", backups_path, class: mobile_nav_link_classes(request.path.start_with?("/backups")) %>
      <%= link_to "Providers", providers_path, class: mobile_nav_link_classes(request.path.start_with?("/providers")) %>
      <%= link_to "Storages", storages_path, class: mobile_nav_link_classes(request.path.start_with?("/storages")) %>
      <%= link_to "Health", health_path, class: mobile_nav_link_classes(request.path.start_with?("/health")) %>
    </div>
  </div>
</nav>
